# Finish the GitHub organization

API cannot create `SILENCE-OBJECTS`. Owner completes the form.

1. Organization name stays `SILENCE.OBJECTS` → URL `https://github.com/SILENCE-OBJECTS`.
2. Contact email: `silence@globalx.pl`.
3. Belongs to: **My personal account** (`ev-silence-owner`) unless legal entity is ready.
4. Tick Terms of Service.
5. Next.

After the org exists:
- Transfer this repo into `SILENCE-OBJECTS`.
- Add `silence@globalx.pl` as billing/security contact.
- Rotate the mailbox password. It appeared in a screenshot and must not stay.

Mail DNS for `globalx.pl` (host requirement): `ns1.aftermarket.hosting`, `ns2.aftermarket.hosting` — only if this domain is meant to serve that mailbox. Do not point `patternlens.app` there.
